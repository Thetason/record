# ================================
# Re:cord 프로덕션 환경변수 설정
# ================================
# 이 파일을 참고하여 Vercel 대시보드에서 환경변수를 설정하세요.
# 절대 실제 값을 이 파일에 입력하지 마세요!

# ================================
# 1. 데이터베이스 (필수)
# ================================
# Neon 또는 Supabase PostgreSQL 사용
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://user:password@host:5432/database?sslmode=require

# ================================
# 2. 인증 (필수)
# ================================
# openssl rand -base64 32 명령으로 생성
NEXTAUTH_SECRET=your-nextauth-secret-here
# 실제 배포 URL로 변경
NEXTAUTH_URL=https://your-domain.vercel.app

# ================================
# 3. 결제 시스템 - 토스페이먼츠 (필수)
# ================================
# https://developers.tosspayments.com 에서 발급
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_xxxxxxxxxxxxx
TOSS_SECRET_KEY=test_sk_xxxxxxxxxxxxx

# ================================
# 4. 이메일 발송 (필수 - 둘 중 하나 선택)
# ================================

# 옵션 A: SendGrid 사용
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com

# 옵션 B: Gmail SMTP 사용
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password

# ================================
# 5. Google Vision API (선택사항 - OCR 기능)
# ================================
# 서비스 계정 JSON을 Base64로 인코딩한 값
GOOGLE_VISION_API_KEY=base64_encoded_service_account_json
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# ================================
# 6. 애플리케이션 설정
# ================================
# 프로덕션 URL
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app

# ================================
# 7. 분석 및 모니터링 (선택사항)
# ================================
# Google Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry (에러 트래킹)
SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ================================
# 환경변수 설정 방법
# ================================
# 1. Vercel 대시보드 접속
# 2. Settings → Environment Variables
# 3. 각 변수를 추가 (Production 환경 선택)
# 4. Save 후 재배포
#
# 주의사항:
# - NEXT_PUBLIC_ 접두사가 있는 변수는 클라이언트에서 접근 가능
# - 민감한 정보는 절대 NEXT_PUBLIC_ 접두사를 사용하지 마세요
# - 모든 변수는 Vercel 대시보드에서 안전하게 관리됩니다